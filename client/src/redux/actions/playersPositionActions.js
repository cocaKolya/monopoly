import axios from 'axios';
import { SET_PLAYERS_POSITION } from '../types/gameTypes';

export const setPlayersPosition = () => async (dispatch) => {
  

  dispatch({
    type: SET_PLAYERS_POSITION,
    payload: 
  });
};
